# 3조 개발 수정요청 사항 — 처리 현황

기준: 노션 [SPEC-INVENTORY · 3조 개발 수정요청 사항 · 1차 수정요청](https://app.notion.com/p/SPEC-INVENTORY-3d62209fcea88009a4baee0a8c4e23ca#3d62209fcea88062986cda1f49a307c7)  
확인: 2026-09-10 · `localhost:3003` 데스크톱 1440×980 폰 프레임

| 표시 | 의미 |
| --- | --- |
| ✅ | 요청대로 반영했고, 화면에서 확인함 |
| ⚠️ | 동작은 맞추었으나 웹 한계·1차 제외가 있음 |
| ❌ | 안 함 (노션 본문 없음 / 1차·웹 제외) |

---

## 0. 웹브라우저

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| FHD / QHD / Mac에서도 모바일 화면 비율 유지. 프레임만 비례 축소 | ✅ | `.phone`에 `aspect-ratio: 390 / 844`, 폭 `min(390px, calc((100dvh - 40px) * 390 / 844))` |
| 극단적으로 좁거나 낮아도 가로·세로로 찌그러지지 않게 | ✅ | 비율 고정 + `max-height: calc(100dvh - 40px)`. 좁은 뷰는 `.stage.mobile`로 프레임 없이 화면만 |

---

## 1. 공통항목 — 탭바

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 드라이브 네비게이션바 아이콘 8장 사용 | ✅ | `public/nav/{home,cal,gift,me}-{on,off}.png` |
| 활성 = 아이콘 스트로크만 `#2F80ED`, 이름 Bold | ✅ | ON PNG에서 연한 원 배경을 잘라 스트로크만 남김. `.tabbar a.on` 색 `#2F80ED` · `font-weight: 800` |
| 비활성 이름은 Bold 하지 말 것 | ✅ | 비활성 `font-weight: 500`, 회색 |

---

## 2. 로그인 & 회원가입

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 이메일 맞고 비밀번호만 틀리면 DB 불일치 토스트. 비밀번호 형식 에러(SP-AU-Login-05)가 뜨면 안 됨 | ✅ | 이메일이 형식에 맞으면 `login()`으로 바로 대조. 빈 비밀번호만 필드 에러 |
| 비밀번호 맞는데 이메일 틀림 / 미가입 이메일도 같은 토스트 | ✅ | 문구 고정: `가입되지 않았거나, 이메일 또는 비밀번호가 일치하지 않습니다.` |
| 토스트 위치·디자인을 피그마처럼 (가운데 어두운 박스) | ✅ | `.toast` `top: 50%` + `translateY(-50%)`, 배경 `#1b1b1b`, 라운드 16 |
| 클라우드가 느려도 토스트가 안 뜨면 안 됨 | ✅ | `loginCloud` 3.5초 타임아웃 후 로컬 계정 대조 |
| 약관 상세 보고 돌아와도 회원가입 입력값 유지 | ✅ | `sessionStorage` 키 `teum:signup-draft` |
| 뒤로가기 위치를 서비스 전체에서 맞춤 (로그인 1·9번 포함) | ✅ | 전역 `.topbar` 왼쪽 16px, `.icon-btn` 36×36. 로그인·목록 모두 셸 기준 16px |
| Enter로 로그인 | ✅ | `<form onSubmit>` + `type="submit"` |

카카오/구글 로그인은 노션 1차가 아니라 명세 1차 제외. 버튼만 두고 「1차 개발 범위에서 제외된 기능이에요.」

---

## 3. 비밀번호 찾기

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 인증코드 불일치 시 6칸 입력값 초기화 | ✅ | `/forgot/verify` 실패 시 digits 비움 |
| `🤔 코드가 오지 않았어요` 문구 제거 | ✅ | 화면에서 삭제. 재전송만 남김 |

실제 메일 OTP는 웹 한계. 코드는 `123456`.

---

## 4. 홈 & 구독목록

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 홈 왼쪽 상단 로고를 드라이브 `홈 메인 로고.png`로 | ✅ | `/brand/home-logo.png` + 「틈」. 히어로에서는 흰색 |
| 알림은 하단 시트가 아니라 오른쪽 사이드시트 | ✅ | `.sheet.side` |
| 홈·혜택 등 모든 화면 스크롤바 숨김 | ✅ | `.scroll { scrollbar-width: none }` + webkit 숨김 |
| `확인하기 ›` 한 줄. 누수 카피는 가능하면 한 줄 | ✅ | `.leak .cta` `white-space: nowrap`. `.leak-copy` 12px + ellipsis |
| FAB 「일상 직접 추가」로 문구 통일 | ✅ | 구독/일상 모두 `… 직접 추가` 길이 맞춤 |
| 직접 추가를 이미지/음성과 구분선으로 분리 | ✅ | `.fab-split` |
| 일상 탭 색 `#FF6B7A` | ✅ | 라이브 `rgb(255, 107, 122)` |
| 카테고리 칩을 데스크톱 마우스 드래그로 넘기기 | ✅ | `ChipScroller` / `bindChipDrag` (10px 이동 후 capture) |
| 배너 랜딩을 이미지와 맞게 | ✅ | 1 AI점검 → `/benefits` · 2 친구초대 → 이동 없음 · 3 결제일 → `/calendar` · 4 구독관리 → `/subscriptions` |
| 배너 `‹ ›`를 이미지 위 좌우로 | ✅ | `.promo-nav` 오버레이 |
| 종 뱃지(빨간 점)가 아이콘과 같이 잘리지 않게 | ✅ | `.bell` / `.home-hero` `overflow: visible` |
| `구독비 점검받기`를 FAB과 같은 줄에 고정 | ✅ | `.inspect-cta` absolute. QA 시 y 차이 약 4px |
| 종 아이콘을 드라이브 `종.png`로 | ✅ | `/icons/bell.png` |

---

## 5. 구독 직접 추가 / 구독 상세

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| AI 인식 후 추출 목록이 아니라 등록 폼에 데이터 넣고 저장 → 상세 | ✅ | `afterExtractPath()` → `/add/confirm` 또는 `/events/new` |
| 결제 주기 = 숫자 + `개월` (월/연/주 드롭다운 아님) | ✅ | `SubForm` 개월 입력 1–99 |
| 구독 상세 하단 탭바 | ✅ | `TabBar` 고정 |
| 무료체험 뱃지 `n개월 무료체험 중` | ✅ | Meitu 등 trial 구독에서 확인 |
| 결제 주기 `n개월` 표기 | ✅ | `everyMonths` 기준. Netflix `1개월` |
| 이용상태 드롭다운이 아래 영역을 밀지 않고 덮게 | ✅ | `.status-pop` `position: absolute`. 헤더 높이 유지. 바깥 클릭 닫힘 |
| 결합 오탐 막기 (Netflix가 결합이면 안 됨) | ✅ | `isBundleLike`는 `parentId` / 네이버플러스·쿠팡와우·배민클럽 / 이름·요금제에 `결합`만 |
| `틈이 확인했어요` 앞에 종 + 관리 위에 배치 | ✅ | `/icons/bell.png` + `.teum-note`를 관리 섹션 위 |
| 정보수정·삭제 위에 「관리」 제목 | ✅ | `관리` 섹션 타이틀 |
| 한글 서비스명에도 드라이브 로고 | ✅ | `lib/brands.ts`. 「유튜브 프리미엄」 → `youtube.png` |

---

## 6. 캘린더

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 일정 상세를 명세처럼 (일시·메모·알림 배너) | ✅ | `/events/[id]`에 일시, 알림 30분 전, `/banners/alarm.png`, 메모, 관리 |
| 날짜 클릭 원이 날짜를 아래로 밀어내지 않게 | ✅ | 선택 원 28×28, 셀 높이 48 고정 |
| 캘린더 색깔 (`#2F80ED`) | ✅ | 선택 채움 `#2F80ED`, 오늘 링 `#2F80ED`, 토요일 파랑, 일요일 빨강 |

---

## 7. 일상 직접 추가

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 알림은 30분 전 고정. 드롭다운으로 바꾸지 말 것 | ✅ | `.when-chip` `pointer-events: none`, 값 30 |

---

## 8-1. 이미지 인식

노션 1차에 제목만 있고 본문 항목은 없음. 랜딩 순서는 **5. 구독 직접 추가**에서 처리.

OS 앨범 권한 화면은 웹 예외로 없음.

---

## 8-2. 음성 인식

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 중단을 눌러도 인식이 계속되는 문제 | ✅ | `abort()` + `stopMic()` |
| 음성 전용 로딩 카피 | ✅ | 「음성을 분석하고 있어요」 |
| 뒤로가기 시 종료 확인 팝업 | ✅ | 내용 있으면 궁금틈끼 확인 |

실마이크·브라우저 권한 시트는 기기/OS 한계.

---

## 9. 혜택 & AI 점검

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 혜택 상세 재구현 (신청 방법 / 이용 조건 / 공식 이동 / 메인으로) | ✅ | `/benefits/[id]` |
| 카테고리 칩 좌우 슬라이드 (잘리지 않게 + 마우스 드래그) | ✅ | `ChipScroller` |
| 카드 혜택은 empty | ✅ | 카드 필터 시 「카드 혜택으로 받을 수 있는 등록된 구독이 없어요.」 + 카드틈끼 |
| 혜택 탭 FAB 삭제 | ✅ | `/benefits`에 FAB 없음 |

---

## 10. 마이페이지 & 설정

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| 버전관리 | ✅ | `/me`에 `앱 버전 0.1.0` |

---

## 11. 콜드스타트 & 엣지케이스

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| (노션 본문 없음) | ❌ | 적을 수정 항목이 없음. 0건 홈·목록은 기존 구현 유지 |

---

## 12. 온보딩

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| (노션 본문 없음) | ❌ | 적을 수정 항목이 없음. 첫 진입 마케팅 동의 팝업은 기존 구현 유지 |

---

## 13. DB 관련

| 요청 | 상태 | 어떻게 처리했는지 |
| --- | --- | --- |
| `services`, `service_keywords`, `bundles`, `bundle_items` | ✅ | `supabase/schema.sql` + `npm run db:schema`로 리모트 적용 |
| `subscriptions`에 `service_id`, `bundle_id`, `subscription_type` | ✅ | 스키마에 컬럼 추가 후 리모트 반영 |
| `email_verifications`, `terms`, `terms_agreements`, `account_deletions` | ✅ | 동일 |
| `benefits`, `benefit_activity`, `ai_inspections` | ✅ | 동일 |
| `ocr_uploads`, `voice_recognitions` | ✅ | 동일 |
| `device_tokens`, `user_search_logs`, `onboarding_progress` | ✅ | 동일 |

리모트에 올라간 테이블: `account_deletions`, `accounts`, `ai_inspections`, `benefit_activity`, `benefits`, `bundle_items`, `bundles`, `device_tokens`, `email_verifications`, `events`, `notices`, `ocr_uploads`, `onboarding_progress`, `service_keywords`, `services`, `subscriptions`, `terms`, `terms_agreements`, `user_search_logs`, `voice_recognitions`

---

## 1차에서 하지 않는 것

노션 1차 본문에 없거나, 명세·PRD에서 웹/1차로 빼 둔 항목입니다.

| 항목 | 이유 |
| --- | --- |
| 카카오 / 구글 OAuth | 명세 1차 제외. 버튼 + 토스트만 |
| OS 푸시 · 상단바 알림 | 웹 불가. 앱 안 알림 목록만 |
| OS 앨범 권한 화면 | 웹 예외. 파일 선택기 |
| 실제 메일 OTP | 웹 한계. 코드 `123456` |
| 마이데이터 · 서비스 연동 | 1차 제외 |
| `.ics` / 외부 캘린더 | 1차 제외 |
| Google 태그 관리자 | ✅ `app/layout.tsx`에 GTM-WL9BD63C + gtag G-1YZTY70NF3 |

---

화면 번호(`SP-AU-Login-01` …) 단위의 했고/부분은 [`SPEC-INVENTORY.md`](./SPEC-INVENTORY.md)에 있습니다.
